
## ⚠️ IMPORTANT: Git Identity

ALL commits MUST be made with this identity:
- **Name:** JulioMCruz
- **Email:** julio.cruz@eb-ms.net

This is already configured globally. Verify with:
```bash
git config user.name   # Should show: JulioMCruz
git config user.email  # Should show: julio.cruz@eb-ms.net
```

NEVER commit as 'Neo Agent' or any other identity.
